---
type: "quiz"
title: "Session 02 — Quiz Cuối giờ"
session: 2
quiz_type: "cuoigio"
sheet_name: "Quiz_CuoiGio_FastAPI"
tags:
  - "type/quiz"
  - "session/02"
deliverable_filename: "Import_Quiz_CuoiGio_Session02"
status: "done"
---

# Session 02 — Quiz Cuối giờ

> 21 câu · lược đồ 12 cột (stage-2 ánh xạ sang .xlsx Quizizz).

| question_content | answer_1 | explanation_answer_1 | answer_2 | explanation_answer_2 | answer_3 | explanation_answer_3 | answer_4 | explanation_answer_4 | isCorrect | difficulty | category |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Trong vòng đời Request-Response, trình tự nào đúng? | Server gửi Request → Client trả Response | Ngược vai trò: Client mới là bên gửi Request. | Client gửi Request → Server xử lý → Server trả Response | Đúng. Client khởi tạo Request, Server phân tích và trả về Response. | Client và Server gửi Request đồng thời | Mô hình Client-Server có vai trò rõ ràng, không gửi Request đồng thời như vậy. | Database gửi Request → Client trả Response | Database không khởi tạo Request tới Client trong mô hình này. | 2 | 4 | Kiến trúc & FastAPI |
| Đặc trưng của kiến trúc Decoupled (Frontend/Backend tách biệt) là gì? | Server chỉ trả dữ liệu thuần (JSON), một API phục vụ được Web, Mobile và bên thứ ba | Đúng. Decoupled tách backend trả JSON thuần, dùng lại cho nhiều loại client. | Server trả về trang HTML đã render đầy đủ cho mỗi request | Đó là mô hình SSR truyền thống, không phải kiến trúc Decoupled. | Frontend và Backend luôn nằm chung một file | Decoupled tách biệt frontend và backend, không gộp chung. | Chỉ phục vụ được trình duyệt web, không dùng cho mobile | Lợi thế của Decoupled chính là phục vụ được cả mobile và bên thứ ba. | 1 | 8 | Kiến trúc & FastAPI |
| Vì sao FastAPI đạt hiệu năng cao tiệm cận NodeJS/Go? | Nhờ dùng chuẩn ASGI bất đồng bộ kết hợp Uvicorn | Đúng. ASGI cho phép xử lý bất đồng bộ, cùng Uvicorn nâng hiệu năng lên cao. | Nhờ biên dịch trực tiếp sang mã máy C | FastAPI không biên dịch sang mã máy C; nó là framework Python. | Nhờ chạy đồng bộ trên WSGI như Flask | WSGI đồng bộ chính là điểm hạn chế mà ASGI khắc phục. | Nhờ loại bỏ hoàn toàn việc dùng Python | FastAPI vẫn là framework Python, không loại bỏ Python. | 1 | 8 | Kiến trúc & FastAPI |
| Trong JSON, cách viết một cặp dữ liệu nào sau đây ĐÚNG chuẩn? | { name: 'An', age: 20 } | Key và chuỗi trong JSON phải bọc nháy kép, không để trần và không nháy đơn. | { "name": "An", "age": 20 } | Đúng. Key và chuỗi bọc nháy kép; số không cần nháy. | { 'name': 'An', 'age': '20' } | JSON không dùng nháy đơn; ngoài ra age là số nên không nên bọc nháy. | { name = "An"; age = 20 } | JSON dùng dấu hai chấm và dấu phẩy, không dùng '=' hay ';'. | 2 | 4 | Kiến trúc & FastAPI |
| Lệnh nào tạo môi trường ảo và cách kích hoạt trên Windows lần lượt là gì? | python -m venv venv  rồi  .\venv\Scripts\activate | Đúng. Tạo bằng 'python -m venv venv', kích hoạt Windows bằng .\venv\Scripts\activate. | pip install venv  rồi  source venv/bin/activate | pip không tạo venv; 'source ...' là cách kích hoạt trên macOS/Linux. | python venv create  rồi  venv start | Cú pháp này không tồn tại. | python -m venv venv  rồi  source venv/bin/activate | Lệnh tạo đúng nhưng 'source venv/bin/activate' là của macOS/Linux, không phải Windows. | 1 | 8 | Môi trường |
| Dấu hiệu nào cho biết môi trường ảo đã được kích hoạt thành công? | Xuất hiện tiền tố (venv) ở đầu dòng lệnh | Đúng. Khi kích hoạt, dấu nhắc lệnh có tiền tố (venv). | Terminal tự đóng lại | Kích hoạt venv không làm đóng terminal. | Python tự gỡ cài đặt | venv không gỡ Python; nó chỉ tạo môi trường riêng. | Máy tính khởi động lại | Kích hoạt venv không khởi động lại máy. | 1 | 4 | Môi trường |
| Cặp lệnh nào dùng để ĐÓNG GÓI và TÁI TẠO danh sách thư viện của dự án? | pip list  và  pip get | 'pip list' chỉ hiển thị; không có 'pip get' để đóng gói/tái tạo. | pip freeze > requirements.txt  và  pip install -r requirements.txt | Đúng. 'pip freeze > requirements.txt' đóng gói, 'pip install -r requirements.txt' tái tạo. | pip save  và  pip load | Không có lệnh 'pip save'/'pip load'. | python freeze  và  python restore | Không có 'python freeze'/'python restore'; đây là chức năng của pip. | 2 | 8 | Môi trường |
| Vì sao nên thêm thư mục venv/ vào file .gitignore? | Để không đẩy môi trường ảo (nặng, máy-cụ thể) lên Git | Đúng. venv/ nặng và phụ thuộc máy; chỉ cần chia sẻ requirements.txt là đủ. | Để Git tự động cài thư viện cho đồng đội | Git không tự cài thư viện; đồng đội tái tạo bằng requirements.txt. | Để mã hóa source code | .gitignore không mã hóa mã nguồn, chỉ loại file khỏi việc theo dõi. | Để tăng tốc độ chạy uvicorn | Việc bỏ qua venv/ không liên quan đến tốc độ uvicorn. | 1 | 4 | Môi trường |
| Đoạn code khởi tạo ứng dụng FastAPI tối thiểu nào sau đây ĐÚNG? | import FastAPI; app = new FastAPI() | Python không dùng 'new' để khởi tạo đối tượng như Java/JS. | from fastapi import FastAPI; app = FastAPI() | Đúng. Nạp lớp FastAPI rồi tạo thực thể app = FastAPI(). | from fastapi import app; FastAPI = app() | Sai thứ tự: phải import lớp FastAPI rồi mới tạo instance app. | require('fastapi'); app = FastAPI() | 'require()' là cú pháp của NodeJS, không phải Python. | 2 | 8 | Khởi tạo app |
| Lệnh nào dùng để chạy ứng dụng FastAPI với chế độ tự khởi động lại khi sửa code? | python main.py --reload | Chạy trực tiếp python main.py không khởi động ASGI server đúng cách. | uvicorn main:app --reload | Đúng. 'uvicorn main:app --reload' (main = file, app = biến instance, --reload = hot reload). | fastapi run main --reload | Đây không phải cú pháp chạy của bài học (dùng uvicorn). | uvicorn app:main --reload | Sai thứ tự: phải là main:app (file:biến), không phải app:main. | 2 | 8 | Khởi tạo app |
| Trong lệnh 'uvicorn main:app --reload', thành phần 'app' chỉ điều gì? | Tên thư mục chứa code | 'app' không phải tên thư mục. | Biến instance FastAPI bên trong file main.py | Đúng. 'main' là tên file, 'app' là biến instance FastAPI trong file đó. | Tên cơ sở dữ liệu | 'app' không liên quan đến tên cơ sở dữ liệu. | Tên cổng mạng | Cổng mạng được chỉ định bằng tham số khác như --port, không phải 'app'. | 2 | 4 | Khởi tạo app |
| Vì sao FastAPI cần Uvicorn để hoạt động? | Vì FastAPI không tự lắng nghe cổng mạng, cần ASGI server vận hành | Đúng. FastAPI là framework, cần một ASGI server (Uvicorn) để lắng nghe và phục vụ request. | Vì Uvicorn dịch code Python sang C | Uvicorn không dịch code sang C. | Vì Uvicorn là cơ sở dữ liệu của FastAPI | Uvicorn là ASGI server, không phải cơ sở dữ liệu. | Vì FastAPI không thể trả JSON nếu thiếu Uvicorn | Khả năng trả JSON thuộc về FastAPI; Uvicorn là lớp server vận hành. | 1 | 8 | Khởi tạo app |
| Mặc định, server Uvicorn lắng nghe ứng dụng FastAPI tại địa chỉ nào? | http://0.0.0.0:80 | Cổng 80 không phải mặc định của Uvicorn dev. | http://127.0.0.1:8000 | Đúng. Mặc định server lắng nghe tại http://127.0.0.1:8000. | http://localhost:3000 | Cổng 3000 là mặc định của nhiều app NodeJS, không phải Uvicorn. | http://192.168.1.1:5000 | Đây là một IP mạng nội bộ tùy chọn, không phải mặc định của Uvicorn. | 2 | 4 | Khởi tạo app |
| Ánh xạ giữa thao tác CRUD và HTTP Method nào sau đây ĐÚNG? | GET=Create, POST=Read | Nhầm lẫn: GET dùng để đọc, POST dùng để tạo. | GET=Read, POST=Create, PUT=Update, DELETE=Delete | Đúng. GET=Read, POST=Create, PUT=Update (toàn bộ), DELETE=Delete. | POST=Delete, DELETE=Create | Sai hoàn toàn: POST là tạo, DELETE là xóa. | PUT=Read, GET=Update | Sai: PUT là cập nhật, GET là đọc. | 2 | 4 | Routing |
| HTTP Method nào dùng để cập nhật MỘT PHẦN tài nguyên (chỉ một vài trường)? | PUT | PUT thường dùng cập nhật TOÀN BỘ tài nguyên. | PATCH | Đúng. PATCH dùng để cập nhật một phần (một vài trường) của tài nguyên. | POST | POST dùng để tạo mới tài nguyên. | GET | GET dùng để đọc dữ liệu, không cập nhật. | 2 | 8 | Routing |
| Để định nghĩa endpoint xử lý request GET tại đường dẫn /hello trong FastAPI, ta dùng gì? | @app.route('/hello') | @app.route là phong cách của Flask; FastAPI dùng decorator theo method. | @app.get('/hello') | Đúng. FastAPI dùng @app.get('/hello') để khai báo endpoint GET. | @app.GET('/hello') | Decorator viết thường '@app.get', không phải '@app.GET'. | app.add('/hello', GET) | Không có cú pháp app.add(...) như vậy trong FastAPI. | 2 | 8 | Routing |
| Endpoint nào đặt tên ĐÚNG chuẩn RESTful cho danh sách sinh viên? | /getStudents | Sai chuẩn: nhúng động từ 'get' vào URL. | /students | Đúng. Dùng danh từ số nhiều '/students', không nhúng động từ. | /student/getAll | Sai chuẩn: nhúng động từ 'getAll' vào URL. | /listStudent | Sai chuẩn: nhúng động từ 'list' và dùng số ít. | 2 | 4 | Routing |
| Routing Engine của FastAPI dựa vào những yếu tố nào để ánh xạ tới đúng hàm xử lý? | URL path kết hợp HTTP Method | Đúng. Routing Engine dò URL path + HTTP method để ánh xạ tới đúng hàm xử lý. | Chỉ dựa vào tên hàm Python | Tên hàm không phải tiêu chí định tuyến; path + method mới là yếu tố chính. | Chỉ dựa vào thứ tự khai báo hàm | Thứ tự khai báo không phải cơ chế định tuyến chính. | Dựa vào cổng mạng đang dùng | Cổng mạng không quyết định việc ánh xạ route. | 1 | 8 | Routing |
| Swagger UI do FastAPI tự sinh nằm ở đường dẫn mặc định nào? | /swagger | Đường dẫn mặc định không phải /swagger. | /docs | Đúng. Swagger UI mặc định tại http://localhost:8000/docs. | /api-docs | /api-docs không phải đường dẫn mặc định của FastAPI. | /redoc | /redoc là giao diện ReDoc, không phải Swagger UI. | 2 | 4 | Swagger UI |
| Đặc điểm 'Zero Configuration' của tài liệu API trong FastAPI nghĩa là gì? | Phải viết file YAML mô tả thủ công cho từng endpoint | Trái ngược với Zero Configuration; FastAPI không bắt viết YAML thủ công. | FastAPI tự đọc cấu trúc hàm và kiểu dữ liệu để sinh tài liệu, không cần cấu hình thêm | Đúng. FastAPI tự sinh tài liệu từ hàm và type hints, không cần cấu hình thêm. | Phải cài Postman mới xem được tài liệu | Swagger UI ngay tại /docs, không cần Postman để xem tài liệu. | Tài liệu chỉ sinh khi deploy lên production | Tài liệu sinh tự động ngay khi chạy dev, không chờ production. | 2 | 8 | Swagger UI |
| Khác biệt chính giữa Swagger UI (/docs) và ReDoc (/redoc) là gì? | Swagger UI cho kiểm thử tương tác (Try it out), ReDoc tối giản tối ưu cho đọc/tra cứu | Đúng. Swagger UI có 'Try it out/Execute' để kiểm thử; ReDoc tối giản, tối ưu cho đọc. | ReDoc cho phép gọi thử API, Swagger UI chỉ để đọc | Ngược lại: Swagger UI mới là nơi gọi thử tương tác. | Cả hai đều không cho gọi thử API | Swagger UI có chức năng gọi thử, nên ý này sai. | Swagger UI dành cho GraphQL, ReDoc dành cho REST | Cả hai đều phục vụ REST/OpenAPI, không phải GraphQL. | 1 | 8 | Swagger UI |

— Thuộc [[Session 02 — MOC]]
