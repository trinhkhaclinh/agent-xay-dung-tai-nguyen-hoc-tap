---
type: "quiz"
title: "Session 02 — Quiz Đầu giờ"
session: 2
quiz_type: "daugio"
sheet_name: "Quiz_DauGio_OOP"
tags:
  - "type/quiz"
  - "session/02"
deliverable_filename: "Import_Quiz_DauGio_Session02"
status: "done"
---

# Session 02 — Quiz Đầu giờ

> 35 câu · lược đồ 12 cột (stage-2 ánh xạ sang .xlsx Quizizz).

| question_content | answer_1 | explanation_answer_1 | answer_2 | explanation_answer_2 | answer_3 | explanation_answer_3 | answer_4 | explanation_answer_4 | isCorrect | difficulty | category |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Từ khóa nào dùng để định nghĩa một hàm trong Python? | function | 'function' là cú pháp của JavaScript, không phải Python. | func | 'func' là từ khóa của Go/Swift, không dùng trong Python. | def | Đúng. Python dùng 'def' để định nghĩa hàm. | define | 'define' không phải từ khóa hợp lệ trong Python. | 3 | 4 | BÀI CŨ |
| Cấu trúc rẽ nhánh nhiều điều kiện trong Python dùng từ khóa nào ở nhánh giữa? | else if | 'else if' là cú pháp của C/Java, Python viết gọn lại. | elseif | 'elseif' là cú pháp của PHP, không dùng trong Python. | elif | Đúng. Python dùng 'elif' cho nhánh điều kiện ở giữa. | switch | Python (trước 3.10) không có 'switch'; match-case mới có sau này. | 3 | 4 | BÀI CŨ |
| Từ khóa nào dùng để thoát ngay khỏi vòng lặp trong Python? | continue | 'continue' chỉ bỏ qua phần còn lại của lần lặp hiện tại rồi sang lần kế tiếp. | break | Đúng. 'break' thoát hoàn toàn khỏi vòng lặp đang chạy. | exit | 'exit' (hay sys.exit) thoát cả chương trình chứ không chỉ vòng lặp. | stop | 'stop' không phải từ khóa của Python. | 2 | 4 | BÀI CŨ |
| Trong vòng lặp, 'continue' có tác dụng gì? | Bỏ qua phần còn lại của lần lặp hiện tại và sang lần lặp kế tiếp | Đúng. 'continue' nhảy ngay sang lần lặp tiếp theo, bỏ qua các lệnh sau nó. | Thoát hoàn toàn khỏi vòng lặp | Đó là tác dụng của 'break', không phải 'continue'. | Dừng toàn bộ chương trình | Đó là tác dụng của sys.exit(), không phải 'continue'. | Lặp lại đúng lần lặp hiện tại từ đầu | 'continue' không quay lại lặp lại lần hiện tại mà tiến tới lần kế tiếp. | 1 | 4 | BÀI CŨ |
| Kiểu dữ liệu nào trong Python là MUTABLE (thay đổi được sau khi tạo)? | tuple | tuple là immutable, không thể thay đổi phần tử sau khi tạo. | str (chuỗi) | Chuỗi (str) là immutable trong Python. | list | Đúng. list là mutable, có thể thêm/sửa/xóa phần tử. | int | int là immutable, mỗi giá trị số là một đối tượng cố định. | 3 | 4 | BÀI CŨ |
| Đặc điểm nào sau đây mô tả đúng về tuple trong Python? | Có thứ tự nhưng không cho phép thay đổi phần tử (immutable) | Đúng. tuple có thứ tự, truy cập theo chỉ số nhưng không sửa được sau khi tạo. | Không có thứ tự và không cho phép trùng lặp | Đó là đặc điểm của set, không phải tuple. | Có thứ tự và thay đổi được tự do như list | tuple không thay đổi được; chỉ list mới sửa được tự do. | Là tập hợp các cặp key-value | Cặp key-value là đặc điểm của dict, không phải tuple. | 1 | 4 | BÀI CŨ |
| Cấu trúc dữ liệu nào trong Python lưu các cặp key-value? | list | list là danh sách có thứ tự, truy cập bằng chỉ số (index), không phải key. | set | set là tập hợp các phần tử duy nhất, không có key-value. | tuple | tuple là dãy có thứ tự cố định, truy cập bằng chỉ số. | dict | Đúng. dict lưu dữ liệu dưới dạng cặp key-value, truy cập bằng key. | 4 | 4 | BÀI CŨ |
| Đặc điểm nào sau đây đúng với set trong Python? | Lưu các phần tử không trùng lặp, không có thứ tự chỉ số | Đúng. set chỉ chứa phần tử duy nhất và không hỗ trợ truy cập theo chỉ số. | Cho phép phần tử trùng lặp và truy cập bằng index | set tự loại bỏ phần tử trùng và không truy cập bằng index. | Lưu cặp key-value như dict | Cặp key-value là đặc điểm của dict, không phải set. | Là dãy immutable giống tuple | set thông thường là mutable (thêm/xóa được), khác tuple immutable. | 1 | 4 | BÀI CŨ |
| Trong định nghĩa hàm, **kwargs nhận tham số ở dạng nào? | Một tuple các đối số vị trí | Đó là *args (một dấu sao) gom đối số vị trí thành tuple. | Một dictionary các đối số dạng từ khóa (key=value) | Đúng. **kwargs gom các đối số dạng key=value thành một dictionary. | Một list các đối số | Python không gom đối số thành list trong cú pháp này. | Một set các giá trị duy nhất | Không có cơ chế gom tham số thành set khi định nghĩa hàm. | 2 | 4 | BÀI CŨ |
| *args trong định nghĩa hàm Python gom các đối số vị trí thành kiểu gì? | list | *args không tạo list mà tạo tuple. | dict | dict là kết quả của **kwargs, không phải *args. | tuple | Đúng. *args gom các đối số vị trí dư thừa thành một tuple. | set | set không phải kết quả của *args. | 3 | 4 | BÀI CŨ |
| Từ khóa nào dùng để tạo một hàm ẩn danh (anonymous function) trong Python? | lambda | Đúng. 'lambda' tạo hàm ẩn danh ngắn gọn trong Python. | anon | 'anon' không phải từ khóa của Python. | func | 'func' không tồn tại trong Python (đó là Go/Swift). | arrow | 'arrow' (=>) là cú pháp hàm mũi tên của JavaScript, không phải Python. | 1 | 4 | BÀI CŨ |
| Từ khóa nào biến một hàm thành generator trong Python? | return | 'return' trả giá trị rồi kết thúc hàm, không tạo generator. | yield | Đúng. 'yield' tạm dừng hàm và trả giá trị từng lần, biến hàm thành generator. | generate | 'generate' không phải từ khóa của Python. | async | 'async' dùng cho lập trình bất đồng bộ (coroutine), không tạo generator thông thường. | 2 | 4 | BÀI CŨ |
| Ưu điểm chính của generator (dùng yield) so với việc trả về một list lớn là gì? | Tính giá trị theo từng lần (lazy), tiết kiệm bộ nhớ | Đúng. generator sinh từng phần tử khi cần nên không nạp toàn bộ vào bộ nhớ. | Luôn chạy nhanh hơn mọi vòng lặp | Tốc độ không phải ưu điểm cốt lõi; lợi ích là bộ nhớ và lazy evaluation. | Tự động sắp xếp dữ liệu | generator không tự sắp xếp dữ liệu. | Không bao giờ kết thúc | generator vẫn kết thúc khi hết phần tử cần sinh. | 1 | 4 | BÀI CŨ |
| Từ khóa nào dùng để định nghĩa một lớp (class) trong Python? | class | Đúng. Python dùng từ khóa 'class' (viết thường) để định nghĩa lớp. | Class | Python phân biệt hoa thường; 'Class' viết hoa không phải từ khóa. | struct | 'struct' là cú pháp của C/Go, không phải Python. | object | 'object' là lớp cơ sở của mọi lớp, không phải từ khóa khai báo lớp. | 1 | 4 | BÀI CŨ |
| Phương thức __init__ trong một class Python có vai trò gì? | Hàm khởi tạo (constructor), gán giá trị ban đầu cho đối tượng | Đúng. __init__ được gọi khi tạo đối tượng để khởi tạo thuộc tính. | Hàm hủy đối tượng (destructor) | Hàm hủy là __del__, không phải __init__. | Hàm in đối tượng ra màn hình | Hàm in/biểu diễn là __str__ hoặc __repr__. | Hàm so sánh hai đối tượng | Hàm so sánh là __eq__, không phải __init__. | 1 | 4 | BÀI CŨ |
| Tham số đầu tiên của một phương thức instance trong class Python thường tên là gì? | this | 'this' là quy ước của Java/C++/JavaScript, không phải Python. | self | Đúng. Python dùng 'self' để tham chiếu chính đối tượng hiện tại. | cls | 'cls' dùng cho classmethod (tham chiếu lớp), không phải phương thức instance thông thường. | me | 'me' chỉ là tên tùy ý, không phải quy ước chuẩn của Python. | 2 | 4 | BÀI CŨ |
| Để lớp Dog kế thừa từ lớp Animal trong Python, ta viết thế nào? | class Dog extends Animal: | 'extends' là cú pháp kế thừa của Java, không phải Python. | class Dog: inherits Animal | 'inherits' không phải cú pháp của Python. | class Dog(Animal): | Đúng. Python kế thừa bằng cách đặt lớp cha trong ngoặc tròn. | class Dog -> Animal: | Cú pháp mũi tên này không tồn tại trong Python. | 3 | 4 | BÀI CŨ |
| Cú pháp list comprehension nào tạo list bình phương các số từ 0 đến 4? | [x*x for x in range(5)] | Đúng. Dùng ngoặc vuông [] tạo list từ biểu thức x*x lặp qua range(5). | {x*x for x in range(5)} | Ngoặc nhọn {} tạo set comprehension, không phải list. | (x*x for x in range(5)) | Ngoặc tròn () tạo generator expression, không phải list. | [for x in range(5): x*x] | Cú pháp này sai; biểu thức phải đứng trước 'for'. | 1 | 4 | BÀI CŨ |
| Cách an toàn để mở và tự động đóng file trong Python là gì? | f = open('a.txt'); ... ; (không cần đóng) | Quên close() dễ gây rò rỉ tài nguyên; cần đóng file thủ công. | with open('a.txt') as f: | Đúng. 'with open(...) as f' tự động đóng file kể cả khi có lỗi. | file.read('a.txt') | Không có hàm file.read(tên_file) như vậy; phải mở file trước. | open('a.txt').close() ngay lập tức | Đóng ngay khi vừa mở thì không đọc/ghi được gì. | 2 | 4 | BÀI CŨ |
| Để ghép phương thức nhận chuỗi viết hoa toàn bộ trong Python là gì? | s.toUpperCase() | toUpperCase() là phương thức của JavaScript, không phải Python. | s.upper() | Đúng. s.upper() trả về bản sao chuỗi viết hoa toàn bộ. | s.capitalize() | capitalize() chỉ viết hoa chữ cái đầu, không phải toàn bộ. | s.uppercase() | uppercase() không phải phương thức chuỗi của Python. | 2 | 4 | BÀI CŨ |
| Phương thức nào tách một chuỗi thành list các từ theo dấu phân cách? | s.join() | join() làm ngược lại: nối list chuỗi thành một chuỗi. | s.split() | Đúng. s.split() tách chuỗi thành list các phần con. | s.append() | append() là phương thức của list, không phải chuỗi. | s.divide() | divide() không phải phương thức chuỗi của Python. | 2 | 4 | BÀI CŨ |
| Kết quả của biểu thức len([1, 2, 3]) + len('ab') trong Python là bao nhiêu? | 6 | len(list) = 3 và len('ab') = 2, tổng là 5 chứ không phải 6. | 5 | Đúng. len([1,2,3]) = 3, len('ab') = 2, cộng lại bằng 5. | 3 | Mới chỉ tính một trong hai len; phải cộng cả hai. | Lỗi vì không cộng được | Cả hai len() đều trả số nguyên nên cộng được bình thường. | 2 | 4 | BÀI CŨ |
| Trong mô hình Client-Server, ai là bên KHỞI TẠO yêu cầu (Request)? | Server | Server là bên nhận và xử lý yêu cầu, rồi trả Response. | Client | Đúng. Client gửi Request, Server phân tích và trả về Response. | Database | Database lưu trữ dữ liệu, thường được Server truy vấn chứ không khởi tạo Request tới Server. | Router mạng | Router chỉ chuyển tiếp gói tin, không phải bên khởi tạo Request ứng dụng. | 2 | 8 | BÀI MỚI |
| Định dạng dữ liệu nhẹ nào thường được dùng để trao đổi giữa Frontend và Backend trong API hiện đại? | XML | XML từng phổ biến nhưng nặng nề hơn; API hiện đại ưu tiên định dạng nhẹ hơn. | JSON | Đúng. JSON là định dạng trao đổi dữ liệu nhẹ, chuẩn cho API hiện đại. | CSV | CSV chỉ phù hợp dữ liệu bảng đơn giản, không biểu diễn tốt dữ liệu lồng nhau. | HTML | HTML dùng để hiển thị trang, không phải để trao đổi dữ liệu thuần giữa hệ thống. | 2 | 8 | BÀI MỚI |
| Trong JSON, giá trị chuỗi (string) phải được bọc bằng ký hiệu nào? | Nháy đơn '...' | JSON không chấp nhận nháy đơn cho chuỗi (đó là thói quen của JS/Python). | Nháy kép "..." | Đúng. Chuẩn JSON yêu cầu key và giá trị chuỗi bọc trong nháy kép. | Dấu backtick `...` | Backtick là template string của JavaScript, không hợp lệ trong JSON. | Không cần dấu nào | Số và boolean không cần nháy, nhưng chuỗi bắt buộc phải có nháy kép. | 2 | 8 | BÀI MỚI |
| FastAPI được xây dựng dựa trên hai thư viện nền tảng nào? | Django và Flask | Django và Flask là các framework độc lập, không phải nền của FastAPI. | Starlette và Pydantic | Đúng. FastAPI = Starlette (web toolkit) + Pydantic (validation bằng type hints). | Requests và NumPy | Requests là thư viện HTTP client, NumPy là tính toán số, không phải nền FastAPI. | Tornado và SQLAlchemy | Tornado và SQLAlchemy không phải nền tảng cốt lõi của FastAPI. | 2 | 8 | BÀI MỚI |
| Sự khác biệt cốt lõi giữa WSGI và ASGI là gì? | WSGI xử lý đồng bộ, ASGI hỗ trợ bất đồng bộ (async/await) | Đúng. WSGI (Django/Flask) xử lý đồng bộ; ASGI hỗ trợ bất đồng bộ, chịu tải cao hơn. | WSGI hỗ trợ async, ASGI chỉ đồng bộ | Ngược lại: ASGI mới là chuẩn hỗ trợ async, không phải WSGI. | Cả hai đều chỉ xử lý đồng bộ | ASGI hỗ trợ bất đồng bộ, không phải cả hai đều đồng bộ. | ASGI chỉ dùng cho frontend, WSGI cho backend | Cả hai đều là chuẩn giao tiếp server-ứng dụng phía backend, không liên quan frontend. | 1 | 8 | BÀI MỚI |
| Uvicorn đóng vai trò gì trong hệ sinh thái FastAPI? | Một ASGI server siêu nhanh để vận hành ứng dụng FastAPI | Đúng. Uvicorn là ASGI server siêu nhanh, giúp hiệu năng FastAPI tiệm cận NodeJS/Go. | Một thư viện validation dữ liệu | Validation là vai trò của Pydantic, không phải Uvicorn. | Một hệ quản trị cơ sở dữ liệu | Uvicorn không phải hệ quản trị cơ sở dữ liệu. | Một trình duyệt để gọi API | Uvicorn là server chạy ứng dụng, không phải trình duyệt. | 1 | 8 | BÀI MỚI |
| Tại sao nên dùng môi trường ảo (virtual environment) cho mỗi dự án Python? | Để cô lập thư viện, tránh xung đột phiên bản giữa các dự án | Đúng. venv cô lập thư viện riêng cho từng dự án, tránh đụng độ phiên bản. | Để tăng tốc CPU khi chạy chương trình | venv không liên quan đến tốc độ CPU. | Để thay thế hoàn toàn việc cài đặt Python | venv vẫn cần một bản Python; nó chỉ tạo môi trường riêng dựa trên Python đã cài. | Để mã hóa source code khỏi bị đọc trộm | venv không mã hóa hay bảo vệ mã nguồn. | 1 | 8 | BÀI MỚI |
| Lệnh nào tạo một môi trường ảo tên 'venv' trong Python? | pip create venv | pip dùng để cài thư viện, không tạo môi trường ảo. | python -m venv venv | Đúng. 'python -m venv venv' tạo thư mục venv/ chứa trình thông dịch và pip riêng. | python venv --new | Không có cú pháp 'python venv --new'. | virtualenv install venv | Cú pháp này sai; công cụ virtualenv dùng khác và đây không phải cách chuẩn. | 2 | 8 | BÀI MỚI |
| Định nghĩa kiến trúc RESTful, cách đặt tên endpoint nào sau đây ĐÚNG chuẩn? | /getStudents | Sai chuẩn REST: nhúng động từ 'get' vào URL; hành động phải do HTTP method xác định. | /students | Đúng. REST đặt tên bằng danh từ số nhiều, không nhúng động từ. | /createNewStudent | Sai chuẩn: nhúng động từ 'create' vào URL. | /student_list_action | Tên rườm rà và nhúng hành động ('action'), không theo chuẩn REST. | 2 | 8 | BÀI MỚI |
| Trong REST, hành động trên tài nguyên được xác định chủ yếu bởi yếu tố nào? | Tên động từ trong URL | REST tránh nhúng động từ vào URL; hành động không nằm ở tên URL. | HTTP Method (GET/POST/PUT/DELETE) | Đúng. HTTP Method quyết định hành động (đọc, tạo, sửa, xóa) trên tài nguyên. | Thứ tự gọi các endpoint | Thứ tự gọi không quyết định hành động của một request. | Cổng (port) của server | Cổng chỉ là nơi server lắng nghe, không xác định hành động REST. | 2 | 8 | BÀI MỚI |
| Giao diện tài liệu API tương tác mà FastAPI tự sinh mặc định tại /docs là gì? | ReDoc | ReDoc nằm tại /redoc, là giao diện đọc tài liệu khác. | Swagger UI | Đúng. Swagger UI tại /docs vừa là tài liệu vừa cho phép kiểm thử tương tác. | Postman | Postman là công cụ riêng cần cài đặt, không phải tài liệu tự sinh tại /docs. | GraphiQL | GraphiQL dành cho GraphQL, không phải REST API của FastAPI. | 2 | 8 | BÀI MỚI |
| Chuẩn quốc tế nào dùng để mô tả REST API mà FastAPI tuân thủ để tự sinh tài liệu? | SOAP | SOAP là giao thức cũ nặng nề dựa trên XML, không phải chuẩn mô tả của FastAPI. | OpenAPI | Đúng. OpenAPI là tiêu chuẩn mô tả REST API mà FastAPI tuân thủ hoàn toàn. | RSS | RSS là định dạng phân phối nội dung, không liên quan mô tả API. | WSDL | WSDL gắn với SOAP/web service cũ, không phải chuẩn của FastAPI. | 2 | 8 | BÀI MỚI |
| Khi một hàm xử lý trong FastAPI trả về một Dictionary, điều gì xảy ra với response? | Báo lỗi vì FastAPI chỉ trả chuỗi | FastAPI không báo lỗi; nó hỗ trợ trả dict rất tự nhiên. | FastAPI tự động chuyển Dictionary thành JSON | Đúng. FastAPI tự động serialize Dictionary thành JSON cho client. | Trả về dưới dạng HTML | Mặc định response là JSON, không phải HTML. | Trả về dưới dạng file nhị phân | Dictionary được chuyển thành JSON text, không phải file nhị phân. | 2 | 8 | BÀI MỚI |

— Thuộc [[Session 02 — MOC]]
