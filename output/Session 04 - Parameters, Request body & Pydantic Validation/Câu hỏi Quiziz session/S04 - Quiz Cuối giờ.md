---
type: "quiz"
title: "Session 04 — Quiz Cuối giờ"
session: 4
quiz_type: "cuoigio"
sheet_name: "Quiz_CuoiGio_FastAPI_04"
tags:
  - "type/quiz"
  - "session/04"
deliverable_filename: ""
status: "done"
---

# Session 04 — Quiz Cuối giờ

> 15 câu · lược đồ 12 cột (stage-2 ánh xạ sang .xlsx Quizizz).

| question_content | answer_1 | explanation_answer_1 | answer_2 | explanation_answer_2 | answer_3 | explanation_answer_3 | answer_4 | explanation_answer_4 | isCorrect | difficulty | category |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Nếu route định nghĩa là /students/{student_id} và hàm xử lý là def get_student(student_id: int), chuyện gì xảy ra khi client gọi GET /students/123? | student_id sẽ nhận giá trị kiểu chuỗi '123' | Không đúng, FastAPI tự ép kiểu sang int theo type hint. | Hệ thống báo lỗi 500 Internal Server Error | Không đúng, request hoàn toàn hợp lệ. | student_id sẽ được ép kiểu thành số nguyên 123 và trả về kết quả thành công | Chính xác, FastAPI ép kiểu thông minh dựa trên type hint. | Hệ thống tự động chuyển hướng sang trang chủ | Không đúng. | 3 | 6 | PATH_PARAMETER |
| Khi định nghĩa route tĩnh /students/me và route động /students/{student_id}, vì sao phải đặt route tĩnh lên trước? | Để code trông đẹp hơn | Không đúng. | Để tránh FastAPI nhận nhầm chuỗi 'me' là giá trị student_id của route động và cố ép sang kiểu int | Chính xác, nếu xếp route động trước, request /students/me sẽ khớp vào {student_id} và trả về lỗi 422 do 'me' không phải int. | Để tăng tốc độ thực thi của máy chủ Uvicorn | Tốc độ thực thi không đổi. | Đó là quy tắc bắt buộc của Python, không xếp trước sẽ lỗi cú pháp Python | Python không kiểm soát thứ tự này, đây là cơ chế định tuyến của FastAPI/Starlette. | 2 | 6 | ROUTE_ORDER |
| Trong hàm def read_items(keyword: str = '', category: str \| None = None), hai tham số này thuộc loại nào? | Path parameters bắt buộc | Không đúng, chúng không có mặt trong đường dẫn route. | Query parameters tùy chọn (optional) | Chính xác, vì chúng không có trong route path và đều có giá trị mặc định ('', None). | Request body bắt buộc | Không đúng. | Query parameters bắt buộc | Không đúng, chúng là tùy chọn vì có giá trị mặc định. | 2 | 6 | QUERY_PARAMETER |
| Để khai báo một Query Parameter là BẮT BUỘC trong FastAPI, chúng ta phải làm thế nào? | Đặt kiểu dữ liệu là Required | Python không có kiểu dữ liệu Required. | Bọc tên tham số trong dấu ngoặc nhọn | Đó là khai báo Path Parameter. | Không gán giá trị mặc định cho tham số đó trong hàm xử lý route | Chính xác, không gán giá trị mặc định nghĩa là client buộc phải truyền giá trị đó trên URL. | Sử dụng decorator @app.required | Không có decorator này. | 3 | 7 | QUERY_PARAMETER |
| FastAPI hỗ trợ ép kiểu logic (bool) từ query parameter. Giá trị nào dưới đây truyền trên URL sẽ được ép kiểu thành False? | 1 | 1 được hiểu là True. | yes | yes được hiểu là True. | 0 | Chính xác, 0, false, no, off đều được FastAPI ép kiểu thành False. | true | true được hiểu là True. | 3 | 5 | QUERY_PARAMETER |
| Khi khai báo class StudentCreate(BaseModel), đối tượng này đóng vai trò gì khi khai báo trong tham số hàm của route POST? | Lấy dữ liệu từ Path Parameter | Không đúng, path parameter khai báo trên URL. | Tự động ánh xạ tới bảng trong database | Đó là ORM model (SQLAlchemy), BaseModel của Pydantic chỉ mô tả schema dữ liệu truyền nhận. | Định nghĩa cấu trúc (schema) và đại diện cho dữ liệu nhận từ Request Body | Chính xác, FastAPI nhận diện tham số kiểu BaseModel và đọc dữ liệu từ thân request (JSON body). | Dùng để render file HTML | Không đúng. | 3 | 6 | REQUEST_BODY |
| Cấu trúc thông báo lỗi 422 Unprocessable Entity của FastAPI gồm 3 trường thông tin chính nào cho mỗi lỗi? | code, message, status | Không đúng. | loc (vị trí), msg (thông báo), type (loại lỗi) | Chính xác, đây là các trường chuẩn của cấu trúc lỗi FastAPI giúp dễ gỡ lỗi. | error, timestamp, path | Không đúng. | file, line, expression | Không đúng. | 2 | 7 | ERROR_HANDLING |
| Ký hiệu '...' (dấu ba chấm / Ellipsis) truyền vào đối số đầu tiên của Path() có ý nghĩa gì? | Tham số này có thể nhận bất kỳ giá trị nào | Không đúng. | Đánh dấu đây là tham số tùy chọn (optional) | Không đúng. | Đánh dấu đây là tham số bắt buộc phải truyền (required) | Chính xác, Ellipsis dùng để báo với FastAPI rằng tham số này là bắt buộc trong cấu trúc validation. | Bỏ qua bước xác thực kiểu dữ liệu | Không đúng. | 3 | 7 | VALIDATION |
| Ràng buộc 'ge=18' trong Field(..., ge=18) của Pydantic model có ý nghĩa gì? | Giá trị thuộc tính phải lớn hơn 18 | Lớn hơn là gt (Greater Than). | Giá trị thuộc tính phải nhỏ hơn hoặc bằng 18 | Nhỏ hơn hoặc bằng là le (Less than or Equal). | Giá trị thuộc tính phải lớn hơn hoặc bằng 18 | Chính xác, ge viết tắt của Greater than or Equal. | Độ dài chuỗi thuộc tính phải bằng 18 ký tự | Độ dài chuỗi là min_length / max_length. | 3 | 5 | VALIDATION |
| Khi cần kiểm tra độ dài tối đa của một chuỗi truyền vào Query Parameter là 50 ký tự, ta khai báo như thế nào? | Query(max_length=50) | Chính xác, max_length dùng cho kiểu dữ liệu chuỗi (string). | Query(le=50) | le dùng cho giới hạn số học (nhỏ hơn hoặc bằng). | Query(lt=50) | lt dùng cho giới hạn số học (nhỏ hơn). | Query(size=50) | Không có tham số size. | 1 | 5 | VALIDATION |
| Để kiểm tra định dạng email của một trường trong Pydantic BaseModel bằng Regex, ta truyền regex vào tham số nào của Field()? | regex | Đã bị loại bỏ trong Pydantic v2. | pattern | Chính xác, trong Pydantic v2 ta sử dụng tham số pattern để chỉ định biểu thức chính quy. | format | Không phải tham số hợp lệ. | match | Không phải tham số hợp lệ. | 2 | 8 | VALIDATION |
| Tham số response_model trong route decorator của FastAPI mang lại lợi ích gì? | Tăng tốc độ truy vấn cơ sở dữ liệu lên gấp đôi | Không tác động trực tiếp tới tốc độ database. | Giúp chuẩn hóa dữ liệu đầu ra và tự động lọc bớt các thông tin nhạy cảm trước khi trả về client | Chính xác, ví dụ giúp ẩn trường mật khẩu của tài khoản người dùng trước khi trả về. | Chuyển đổi giao diện sang chế độ tối | Không liên quan. | Tự động mã hóa toàn bộ dữ liệu trả về thành dạng nhị phân | Dữ liệu vẫn là JSON văn bản. | 2 | 7 | RESPONSE_MODEL |
| Sự khác biệt lớn nhất về mặt nghiệp vụ giữa Path Parameter và Query Parameter là gì? | Path Parameter chỉ nhận số, Query Parameter chỉ nhận chữ | Cả hai đều có thể nhận bất kỳ kiểu dữ liệu nào. | Path Parameter định danh duy nhất tài nguyên, mang tính bắt buộc; Query Parameter dùng để lọc/sắp xếp/tìm kiếm tài nguyên, mang tính tùy chọn | Chính xác, đây là quy chuẩn thiết kế RESTful chuẩn hóa toàn cầu. | Path Parameter chạy nhanh hơn Query Parameter | Hiệu năng xử lý tương đương nhau. | Query Parameter bắt buộc phải viết mã hóa bảo mật hơn | Mức độ bảo mật như nhau. | 2 | 6 | REST_DESIGN |
| Hàm Field() được import từ thư viện nào? | fastapi | Fastapi cung cấp Query() và Path(). | typing | Typing cung cấp các type hint như Optional. | pydantic | Chính xác, Field() được cung cấp bởi pydantic. | starlette | Starlette là framework core ở dưới. | 3 | 5 | VALIDATION |
| Nếu client gửi JSON có thêm một số trường lạ không khai báo trong Pydantic BaseModel, FastAPI sẽ xử lý như thế nào mặc định? | Báo lỗi 422 Unprocessable Entity lập tức | Không báo lỗi. | Bỏ qua các trường thừa đó và chỉ lấy các trường có khai báo trong Model | Chính xác, mặc định Pydantic lọc bỏ các trường không được định nghĩa trong schema. | Crash toàn bộ ứng dụng server | Server vẫn hoạt động bình thường. | Tự động thêm các trường đó vào database | FastAPI không tự lưu database. | 2 | 8 | REQUEST_BODY |

— Thuộc [[Session 04 — MOC]]
